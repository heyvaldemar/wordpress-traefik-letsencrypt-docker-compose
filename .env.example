# WordPress + Traefik + Let's Encrypt — Environment Variables
# Copy this file to `.env` and fill in each value before `docker compose up`.
# `.env` is gitignored — never commit real credential values.
#
# Image versions are NOT set here. The tested tag + sha256 digest for every
# image lives in the compose file's `x-images` block, so `git pull` delivers
# the version combination this repository has tested. To pin a different
# version yourself, uncomment and set:
# TRAEFIK_IMAGE_TAG=traefik:3.7@sha256:…
# WORDPRESS_MARIADB_IMAGE_TAG=mariadb:11.4@sha256:…
# WORDPRESS_IMAGE_TAG=wordpress:7.1.0@sha256:…

# ──────────────────────────────────────────────────────────────────────────────
# Traefik reverse proxy
# ──────────────────────────────────────────────────────────────────────────────

# Email address for Let's Encrypt renewal notices. REQUIRED.
TRAEFIK_ACME_EMAIL=admin@example.com

# Hostname for the Traefik dashboard (basic-auth protected). REQUIRED.
TRAEFIK_HOSTNAME=traefik.www.example.com

# Basic Authentication for the Traefik dashboard.
# Format: USERNAME:BCRYPT_HASH (with $ characters doubled to $$ for compose)
#     htpasswd -nbB traefikadmin YOUR_STRONG_PASSWORD | sed 's/\$/\$\$/g'
# REQUIRED — do not ship production without changing this.
TRAEFIK_BASIC_AUTH=change_me_generate_with_htpasswd_and_replace_this_string

# Log level: DEBUG, INFO, WARN, ERROR. Default: WARN.
# TRAEFIK_LOG_LEVEL=WARN

# ──────────────────────────────────────────────────────────────────────────────
# WordPress
# ──────────────────────────────────────────────────────────────────────────────

# Public hostname for the site. Must resolve to this server's public IP for
# the Let's Encrypt TLS-ALPN challenge. REQUIRED.
WORDPRESS_HOSTNAME=www.example.com

# REQUIRED — generate each with:
#     openssl rand -base64 24 | tr -d '/+=' | head -c 32
WORDPRESS_DB_PASSWORD=change_me_before_first_start
WORDPRESS_DB_ADMIN_PASSWORD=change_me_before_first_start

# The site title and admin account are created in the WordPress web
# installer on first visit — open the site right after deploy and complete
# it before sharing the URL.

# Defaults below are set in the compose file; override only to diverge.
# WORDPRESS_DB_NAME=wordpressdb
# WORDPRESS_DB_USER=wordpressdbbuser
# WORDPRESS_TABLE_PREFIX=wpapp_

# ──────────────────────────────────────────────────────────────────────────────
# Backups (defaults shown; override only to tune)
# ──────────────────────────────────────────────────────────────────────────────

# BACKUP_INIT_SLEEP=30m
# BACKUP_INTERVAL=24h
# MARIADB_BACKUP_PRUNE_DAYS=30
# DATA_BACKUP_PRUNE_DAYS=30
# MARIADB_BACKUPS_PATH=/srv/wordpress-mariadb/backups
# DATA_BACKUPS_PATH=/srv/wordpress-application-data/backups
# DATA_PATH=/var/www/html
# MARIADB_BACKUP_NAME=wordpress-mariadb-backup
# DATA_BACKUP_NAME=wordpress-application-data-backup
